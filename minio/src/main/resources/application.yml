minio:
  endpoint: http://172.0.0.1:9000
  access-key: admin
  secret-key: minioAdmin
  bucket-name: bucket-test
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB